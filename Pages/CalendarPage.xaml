<Page x:Class="PRNProject.Pages.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PRNProject.Pages"
      xmlns:components="clr-namespace:PRNProject.Pages.Components"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="CalendarPage"
      Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="HeaderForegroundBrush" Color="#172B4D"/>
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#5E6C84"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="CalendarTaskItemTemplate">
            <Border Background="{StaticResource CardBackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1" CornerRadius="4" 
            Padding="10" Margin="0,3">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" 
                       Foreground="{StaticResource HeaderForegroundBrush}" 
                       TextWrapping="Wrap"/>

                    <TextBlock Text="{Binding Project.Title, FallbackValue='Không có dự án'}" 
                       Foreground="{StaticResource SubtleTextBrush}" 
                       FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <DockPanel LastChildFill="True">

        <components:Sidebar x:Name="SidebarControl" DockPanel.Dock="Left"/>

        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Calendar x:Name="TaskCalendar" Grid.Column="0" 
                      SelectedDatesChanged="TaskCalendar_SelectedDatesChanged"
                      Margin="0,0,20,0"
                      VerticalAlignment="Top"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" x:Name="SelectedDateTextBlock" 
                               Text="Nhiệm vụ sắp tới" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="{StaticResource HeaderForegroundBrush}"
                               VerticalAlignment="Center"/>

                    <Button x:Name="ShowAllButton" Grid.Column="1" 
                            Content="Hiển thị tất cả" 
                            Style="{StaticResource LinkButtonStyle}"
                            Click="ShowAllButton_Click"
                            VerticalAlignment="Center"/>
                </Grid>

                <ListView x:Name="TasksForDayListView" Grid.Row="1"
                          ItemTemplate="{StaticResource CalendarTaskItemTemplate}"
                          BorderThickness="0" Background="Transparent"
                          HorizontalContentAlignment="Stretch"/>

                <TextBlock x:Name="NoTasksTextBlock" Grid.Row="1" 
                           Text="Không có nhiệm vụ nào."
                           FontSize="14" Foreground="{StaticResource SubtleTextBrush}"
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>
        </Grid>

    </DockPanel>
</Page>